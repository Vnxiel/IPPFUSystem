document.addEventListener("DOMContentLoaded", function () {
    let modal = new bootstrap.Modal(document.getElementById("addNewProjectModal"));
    console.log(modal); // Check if modal instance is created
});